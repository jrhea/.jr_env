---@type LazySpec
return {
  "saghen/blink.cmp",
  opts = {
    completion = {
      menu = {
        -- only show completion menu when manually triggered (e.g. <C-space>)
        auto_show = false,
      },
    },
  },
}
